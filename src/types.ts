export type MachineState='IDLE'|'SOUND_PENDING'|'ACTIVE'|'SILENCE_TIMER'|'MANUAL_COOLDOWN';
export interface CardConfig {type:string; camera?:string; stream?:string; sound_sensor:string; id?:string; preload?:boolean; auto_open?:boolean; sound_threshold_db?:number; sound_reset_db?:number; trigger_for?:number; silence_duration?:number; manual_close_cooldown?:number; debug?:boolean; kiosk?:{enabled?:boolean;device_bound?:boolean;allowed_user_ids?:string[]};audio?:{default_muted?:boolean;remember_state?:boolean};controls?:{mute?:boolean;close?:boolean;volume?:boolean;brightness?:boolean}; companion?:{notify_service?:string;active_brightness?:number;idle_brightness?:number;active_volume?:number;idle_volume?:number};}
export interface Hass {states:Record<string,{state:string;attributes:Record<string,unknown>}>;user?:{id:string};callService(domain:string,service:string,data?:Record<string,unknown>):Promise<unknown>;}
export const defaults={preload:true,trigger_for:1,silence_duration:5,manual_close_cooldown:30,sound_threshold_db:48,sound_reset_db:42};
